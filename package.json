{
  "name": "langgraph-agentic-rag",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "db:bootstrap": "bash scripts/db-bootstrap.sh",
    "backend:start": "bash scripts/start-supabase.sh && docker compose up --build backend",
    "backend:lint": "docker compose run --rm backend ruff check --fix .",
    "backend:format": "docker compose run --rm backend ruff format .",
    "backend:test": "docker compose run --rm backend pytest",
    "frontend:dev": "bun run --filter frontend dev",
    "frontend:lint": "bun run --filter frontend lint",
    "frontend:test": "bun run --filter frontend test"
  },
  "devDependencies": {
    "supabase": "2.114.0"
  }
}
